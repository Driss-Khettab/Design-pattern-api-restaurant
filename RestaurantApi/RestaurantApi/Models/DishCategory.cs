namespace RestaurantApi.Models
{
    // Les 4 catégories de plats du restaurant (remplace la string naïve du squelette).
    public enum DishCategory
    {
        Starter,
        MainCourse,
        Dessert,
        Beverage
    }
}
