# Diagrammes UML

Les diagrammes sont fournis directement dans le README principal, ecrits en **Mermaid** :

- Diagramme de classes global (tous les patterns, avec le role de chaque classe)
- Diagramme d'etats de la commande (workflow du pattern State)
- Un zoom par pattern (Factory, Strategy, State, Observer, Singleton)

## Export en image (si un PNG/SVG est exige au rendu)

1. Copier le bloc Mermaid concerne depuis le README.
2. Le coller sur https://mermaid.live
3. Exporter en PNG ou SVG et deposer l'image ici.
